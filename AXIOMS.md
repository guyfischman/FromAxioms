# Axioms

<!-- generated by `python3 tools/audit.py --markdown`; do not edit -->

Every one of the 698 declarations in the library, asked what it depends on.

| axioms | declarations |
|---|---|
| `propext, Quot.sound` | 478 |
| `none` | 167 |
| `Quot.sound` | 19 |
| `propext` | 17 |
| `em` | 10 |
| `choice` | 4 |
| `funext` | 2 |
| `propext, Classical.choice, Quot.sound` | 1 |

## Registered costs (15)

| declaration | axioms | why |
|---|---|---|
| `Analysis.realMul` | `propext, Classical.choice, Quot.sound` | reversed by `Constructive.em_of_realNonneg_or`, whose hypothesis is the SIGN DISJUNCTION and not the product. The `Classical.choice` here is spent by the `if` on an undecidable Prop: proving by cases needs em, defining by cases needs a `Decidable`, and em supplies no such instance -- `Or` does not eliminate into data. That blocks this route and not every route. `realMulNonneg` is already a single separation with no branch and is choice-free, so whether a sign-split-free definition of the general product costs less is OPEN here |
| `Exists.witness` | `choice` | reversed by `choice_of_witness`. Phase 1. Together with witness_spec it reconstructs the axiom, so it is choice rather than a consequence of it |
| `Exists.witness_spec` | `choice` | reversed by `choice_of_witness`. Phase 1. The property half of the same reconstruction |
| `byCases` | `em` | reversed by `em_of_dne`. Pinned: Phase 1. Case analysis on an arbitrary proposition -- em applied |
| `byContradiction` | `em` | reversed by `em_of_byContradiction`. Pinned: Phase 1. dne under another name |
| `choice` | `choice` | Phase 1 declares it. Reconstructed by choice_of_witness, so Exists.witness is exactly this |
| `contrapose'` | `em` | reversed by `em_of_contrapose'`. Pinned: Phase 1. dne in disguise |
| `decider_of_em` | `choice` | reversed by `em_of_decider`. Phase 1. The gap between proving by cases and defining by cases, measured: em is a hypothesis here, not an axiom, so [choice] is what turning Or p (Not p) into data costs. em_of_decider is the axiom-free converse. Phase 2's realMul, dcNum and dcDigit all sit on this |
| `dne` | `em` | reversed by `em_of_dne`. Pinned: Phase 1. Equivalent to em, not weaker |
| `drinker` | `em` | reversed by `wem_of_drinker`. Phase 1. Same two-element family as exists_not_of_not_forall, same WEM |
| `em` | `em` | Phase 1 declares it. The thing under audit, not a consequence of it |
| `exists_not_of_not_forall` | `em` | reversed by `wem_of_exists_not_of_not_forall`. Phase 1. Reverses to WEM, not em: the witness lands on Not a or on Not (Not a), and the second is where em would be needed. The earlier attempts failed because they aimed at em |
| `imp_iff_not_or` | `em` | reversed by `em_of_imp_iff_not_or`. Pinned: Phase 1. Reverses at a := b |
| `not_and_or` | `em` | reversed by `wem_of_not_and_or`. Pinned: Phase 1. De Morgan's third law -- weak excluded middle, not em. Same split as sdiff_inter one phase later |
| `peirce` | `em` | reversed by `em_of_peirce`. Pinned: Phase 1. Reverses with b := False |
